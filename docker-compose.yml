version: '3.8'

services:
  nuxt-app:
    build: .
    ports:
      - "9801:9801"
    environment:
      NITRO_PORT: 9801
      NITRO_HOST: 0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped